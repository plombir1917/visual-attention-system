{
  "name": "vas-desktop",
  "version": "1.0.0",
  "description": "Visual Attention System — Desktop Client",
  "main": "./out/main/index.js",
  "author": "VAS",
  "private": true,
  "scripts": {
    "dev": "electron-vite dev",
    "build": "vue-tsc --noEmit && electron-vite build",
    "dist:win": "npm run build && electron-builder --win",
    "preview": "electron-vite preview"
  },
  "dependencies": {
    "pinia": "^2.3.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "electron": "^33.4.0",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "typescript": "^5.7.2",
    "vue-tsc": "^2.2.0"
  }
}
